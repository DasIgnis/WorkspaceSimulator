<Window x:Class="EmotionalBlackboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmotionalBlackboard"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="6*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <ListBox x:Name="messagesList"
                 Margin="10"></ListBox>

        <ListBox x:Name="employeeList"
                 Margin="10"
                 Grid.Column="1"></ListBox>

        <ListBox x:Name="iterationResult"
                 Margin="10"
                 Grid.Column="2"></ListBox>

        <Button x:Name="act"
                Grid.Row="1"
                Grid.Column="1"
                Margin="10"
                Content="Новый день"
                Click="act_Click"></Button>
    </Grid>
</Window>
